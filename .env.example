# Copy to .env.local for local dev (Next.js convention).
# On Vercel, set this in Project Settings → Environment Variables instead of
# committing a real file.

NEXT_PUBLIC_INGEST_API_URL=https://your-ingest-api.example.com
